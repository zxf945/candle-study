[package]
name = "candle-study"
version = "0.1.0"
edition = "2024"


[dependencies]
#提供张量和基本操作
candle-core = { version = "0.9.1", features = [cuda] }
#提供神经网络层、优化器和损失函数
candle-nn = { version = "0.9.1", features = [cuda] }
#用于数据加载时的随机打乱
rand = "0.9.2"
